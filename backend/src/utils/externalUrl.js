'use strict';const HttpError=require('./httpError');function validateExternalUrl(value){let parsed;try{parsed=new URL(String(value).trim());}catch{throw new HttpError(400,'Enter a valid HTTPS external URL.');}if(parsed.protocol!=='https:'||parsed.username||parsed.password)throw new HttpError(400,'External resources must use HTTPS.');return parsed.href;}module.exports={validateExternalUrl};
